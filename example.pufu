# Ejemplo de código Pufu
# Este código suma dos números y muestra el resultado

main:
    load reg.1 64
    load reg.2 c8

    # Sumar
    add reg.1 reg.2 
    
    # Guardar resultado
    store reg.1 12c

    # Mostrar resultado
    syscall (write)
    load    reg.1
    syscall (exit)
